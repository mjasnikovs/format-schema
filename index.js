const {
	format,
	stringFormat,
	intFormat,
	floatFormat,
	booleanFormat,
	STRING,
	INTEGER,
	FLOAT,
	BOOLEAN
} = require('./src/format')

const graphqlFormat = require('./src/graphqlFormat')

module.exports = {
	format,
	graphqlFormat,
	stringFormat,
	intFormat,
	floatFormat,
	booleanFormat,
	STRING,
	INTEGER,
	FLOAT,
	BOOLEAN
}
