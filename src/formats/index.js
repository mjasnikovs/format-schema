const stringFormat = require('./string')
const integerFormat = require('./integer')
const floatFormat = require('./float')
const booleanFormat = require('./boolean')

module.exports = {
	stringFormat,
	integerFormat,
	floatFormat,
	booleanFormat
}
