const format = require('./format')
const promiseFormat = require('./promiseFormat')

const {
	stringFormat,
	integerFormat,
	floatFormat,
	booleanFormat
} = require('./formats')

module.exports = {
	format,
	promiseFormat,
	stringFormat,
	integerFormat,
	floatFormat,
	booleanFormat
}
