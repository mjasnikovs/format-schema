const format = require('./format')

const {
	stringFormat,
	integerFormat,
	floatFormat,
	booleanFormat
} = require('./formats')

module.exports = {
	format,
	stringFormat,
	integerFormat,
	floatFormat,
	booleanFormat
}
